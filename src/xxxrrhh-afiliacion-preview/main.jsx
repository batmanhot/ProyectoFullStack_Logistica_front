import { useMemo, useState } from 'react'
import { createRoot } from 'react-dom/client'
import {
  ArrowLeft, ArrowRight, BadgeCheck, BriefcaseBusiness, Check, CheckCircle2,
  ChevronDown, CircleHelp, FileText, HeartPulse, Landmark, Mail, Phone,
  ShieldCheck, Sparkles, UserRound, UsersRound,
} from 'lucide-react'
import '../index.css'

const STEPS = [
  { id:1, label:'Datos personales', icon:UserRound },
  { id:2, label:'Afiliaciones', icon:HeartPulse },
  { id:3, label:'Revisión', icon:BadgeCheck },
]

const initialForm = {
  tipoDocumento:'DNI', documento:'', nombres:'', apellidos:'', correo:'', telefono:'',
  cargo:'Analista de operaciones', sede:'Lima · Oficina central', fechaIngreso:'2026-09-01',
  modalidad:'Tiempo completo', regimen:'Régimen general', sistemaPension:'AFP', afp:'Integra',
  cuspp:'', seguroSalud:'EsSalud', eps:'', tieneDependientes:false, dependientes:'0',
}

function Field({ label, hint, children, className = '' }) {
  return <label className={`block ${className}`}><span className="text-[12px] font-semibold text-slate-700">{label}</span>{hint && <span className="ml-1 text-[11px] text-slate-400">{hint}</span>}<div className="mt-1.5">{children}</div></label>
}

const control = 'h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm text-slate-800 outline-none transition placeholder:text-slate-300 focus:border-violet-500 focus:ring-4 focus:ring-violet-100'

function Select({ children, ...props }) { return <div className="relative"><select {...props} className={`${control} appearance-none pr-9`}>{children}</select><ChevronDown size={16} className="pointer-events-none absolute right-3 top-3 text-slate-400" /></div> }

function Stepper({ step, goTo }) {
  return <ol className="mb-8 grid grid-cols-3 gap-2" aria-label="Progreso de afiliación">{STEPS.map(({ id, label, icon:Icon }) => <li key={id}><button type="button" disabled={id > step} onClick={() => goTo(id)} className={`flex w-full flex-col items-start gap-2 rounded-2xl p-3 text-left transition ${id === step ? 'bg-violet-50' : id < step ? 'hover:bg-slate-50' : 'cursor-not-allowed'}`}><span className={`flex h-8 w-8 items-center justify-center rounded-full text-xs font-bold ${id < step ? 'bg-emerald-500 text-white' : id === step ? 'bg-violet-600 text-white shadow-lg shadow-violet-200' : 'bg-slate-100 text-slate-400'}`}>{id < step ? <Check size={16} /> : id}</span><span><span className={`block text-xs font-semibold ${id <= step ? 'text-slate-800' : 'text-slate-400'}`}>{label}</span><span className="mt-0.5 hidden text-[11px] text-slate-400 sm:block">{id === 1 ? 'Identificación' : id === 2 ? 'Cobertura' : 'Confirmación'}</span></span></button></li>)}</ol>
}

function PanelTitle({ icon:Icon, eyebrow, title, description }) {
  return <div className="mb-6"><div className="flex items-center gap-2 text-violet-600"><Icon size={17} strokeWidth={2.4} /><span className="text-[11px] font-bold uppercase tracking-[.14em]">{eyebrow}</span></div><h2 className="mt-3 text-xl font-semibold tracking-[-.02em] text-slate-900">{title}</h2><p className="mt-1 text-sm leading-6 text-slate-500">{description}</p></div>
}

function PersonalStep({ form, set }) {
  return <><PanelTitle icon={UserRound} eyebrow="Paso 1 de 3" title="¿A quién estamos afiliando?" description="Ingresa únicamente los datos necesarios para identificar y contactar a la persona." />
    <div className="grid gap-4 sm:grid-cols-2"><Field label="Tipo de documento"><Select value={form.tipoDocumento} onChange={e => set('tipoDocumento',e.target.value)}><option>DNI</option><option>Carné de extranjería</option><option>Pasaporte</option></Select></Field><Field label="Número de documento"><input autoFocus value={form.documento} onChange={e => set('documento',e.target.value.replace(/\D/g,''))} inputMode="numeric" className={control} placeholder="Ej. 72648159" /></Field><Field label="Nombres"><input value={form.nombres} onChange={e => set('nombres',e.target.value)} className={control} placeholder="Ej. Valeria Sofía" /></Field><Field label="Apellidos"><input value={form.apellidos} onChange={e => set('apellidos',e.target.value)} className={control} placeholder="Ej. Paredes Luna" /></Field><Field label="Correo corporativo"><div className="relative"><Mail size={16} className="absolute left-3 top-3 text-slate-400" /><input value={form.correo} onChange={e => set('correo',e.target.value)} type="email" className={`${control} pl-10`} placeholder="nombre@empresa.com" /></div></Field><Field label="Celular"><div className="relative"><Phone size={16} className="absolute left-3 top-3 text-slate-400" /><input value={form.telefono} onChange={e => set('telefono',e.target.value)} inputMode="tel" className={`${control} pl-10`} placeholder="999 000 000" /></div></Field></div>
    <div className="my-7 border-t border-slate-100" /><p className="mb-4 text-[12px] font-bold uppercase tracking-[.12em] text-slate-400">Información laboral</p><div className="grid gap-4 sm:grid-cols-2"><Field label="Cargo"><input value={form.cargo} onChange={e => set('cargo',e.target.value)} className={control} /></Field><Field label="Sede"><Select value={form.sede} onChange={e => set('sede',e.target.value)}><option>Lima · Oficina central</option><option>Arequipa · Operaciones</option><option>Trujillo · Almacén</option></Select></Field><Field label="Fecha de ingreso"><input type="date" value={form.fechaIngreso} onChange={e => set('fechaIngreso',e.target.value)} className={control} /></Field><Field label="Modalidad"><Select value={form.modalidad} onChange={e => set('modalidad',e.target.value)}><option>Tiempo completo</option><option>Tiempo parcial</option><option>Practicante</option></Select></Field></div>
  </>
}

function ChoiceCard({ active, onClick, icon:Icon, title, description, badge }) {
  return <button type="button" onClick={onClick} className={`relative rounded-2xl border p-4 text-left transition ${active ? 'border-violet-500 bg-violet-50 shadow-sm' : 'border-slate-200 bg-white hover:border-slate-300 hover:bg-slate-50'}`}><div className={`flex h-9 w-9 items-center justify-center rounded-xl ${active ? 'bg-violet-600 text-white' : 'bg-slate-100 text-slate-500'}`}><Icon size={18} /></div><p className="mt-3 text-sm font-semibold text-slate-900">{title}</p><p className="mt-1 text-xs leading-5 text-slate-500">{description}</p>{badge && <span className="absolute right-3 top-3 rounded-full bg-white px-2 py-1 text-[10px] font-bold text-violet-600 shadow-sm">{badge}</span>}{active && <span className="absolute bottom-3 right-3 flex h-5 w-5 items-center justify-center rounded-full bg-violet-600 text-white"><Check size={12} strokeWidth={3}/></span>}</button>
}

function AffiliationStep({ form, set }) {
  return <><PanelTitle icon={HeartPulse} eyebrow="Paso 2 de 3" title="Configura sus afiliaciones" description="Define cobertura de salud y sistema previsional. Puedes actualizar esta información después." />
    <div><p className="mb-3 text-[12px] font-bold uppercase tracking-[.12em] text-slate-400">Sistema de pensiones</p><div className="grid gap-3 sm:grid-cols-2"><ChoiceCard active={form.sistemaPension==='AFP'} onClick={()=>set('sistemaPension','AFP')} icon={Landmark} title="AFP" description="Fondo privado de pensiones" badge="Más común"/><ChoiceCard active={form.sistemaPension==='ONP'} onClick={()=>set('sistemaPension','ONP')} icon={ShieldCheck} title="ONP" description="Sistema nacional de pensiones"/></div>{form.sistemaPension==='AFP' && <div className="mt-4 grid gap-4 rounded-2xl bg-slate-50 p-4 sm:grid-cols-2"><Field label="AFP elegida"><Select value={form.afp} onChange={e=>set('afp',e.target.value)}><option>Integra</option><option>Prima</option><option>Profuturo</option><option>Habitat</option></Select></Field><Field label="Código CUSPP" hint="Opcional"><input value={form.cuspp} onChange={e=>set('cuspp',e.target.value)} className={control} placeholder="Ej. 123456VPL" /></Field></div>}</div>
    <div className="my-7 border-t border-slate-100" />
    <div><p className="mb-3 text-[12px] font-bold uppercase tracking-[.12em] text-slate-400">Cobertura de salud</p><div className="grid gap-3 sm:grid-cols-2"><ChoiceCard active={form.seguroSalud==='EsSalud'} onClick={()=>set('seguroSalud','EsSalud')} icon={HeartPulse} title="EsSalud" description="Cobertura obligatoria de salud"/><ChoiceCard active={form.seguroSalud==='EPS'} onClick={()=>set('seguroSalud','EPS')} icon={Sparkles} title="EPS" description="Plan complementario privado" /></div>{form.seguroSalud==='EPS' && <div className="mt-4 rounded-2xl bg-slate-50 p-4"><Field label="Entidad prestadora de salud"><input value={form.eps} onChange={e=>set('eps',e.target.value)} className={control} placeholder="Ej. Pacífico EPS" /></Field></div>}</div>
    <div className="mt-7 flex items-center justify-between rounded-2xl border border-slate-200 p-4"><div className="flex gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-amber-50 text-amber-600"><UsersRound size={18}/></div><div><p className="text-sm font-semibold text-slate-800">¿Tiene derechohabientes?</p><p className="mt-0.5 text-xs text-slate-500">Cónyuge, hijos u otros familiares registrados.</p></div></div><button type="button" aria-pressed={form.tieneDependientes} onClick={()=>set('tieneDependientes',!form.tieneDependientes)} className={`relative h-7 w-12 rounded-full transition ${form.tieneDependientes?'bg-violet-600':'bg-slate-200'}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white shadow transition ${form.tieneDependientes?'left-6':'left-1'}`} /></button></div>
  </>
}

function ReviewStep({ form }) {
  const name = `${form.nombres} ${form.apellidos}`.trim() || 'Colaborador pendiente'
  return <><PanelTitle icon={BadgeCheck} eyebrow="Paso 3 de 3" title="Revisa antes de registrar" description="Confirma los datos principales. Luego podrás editar el registro desde el perfil del colaborador." /><div className="overflow-hidden rounded-2xl border border-slate-200"><section className="border-b border-slate-100 p-5"><p className="text-[11px] font-bold uppercase tracking-[.12em] text-slate-400">Colaborador</p><p className="mt-2 text-lg font-semibold text-slate-900">{name}</p><p className="mt-1 text-sm text-slate-500">{form.tipoDocumento} {form.documento || '—'} · {form.cargo}</p></section><section className="grid gap-5 border-b border-slate-100 p-5 sm:grid-cols-2"><div><p className="text-[11px] font-bold uppercase tracking-[.12em] text-slate-400">Pensiones</p><p className="mt-2 font-semibold text-slate-800">{form.sistemaPension}{form.sistemaPension==='AFP' ? ` · ${form.afp}` : ''}</p><p className="mt-1 text-xs text-slate-500">CUSPP: {form.cuspp || 'Por registrar'}</p></div><div><p className="text-[11px] font-bold uppercase tracking-[.12em] text-slate-400">Salud</p><p className="mt-2 font-semibold text-slate-800">{form.seguroSalud}{form.seguroSalud==='EPS' && form.eps ? ` · ${form.eps}` : ''}</p><p className="mt-1 text-xs text-slate-500">Derechohabientes: {form.tieneDependientes ? 'Sí' : 'No'}</p></div></section><section className="p-5"><p className="text-[11px] font-bold uppercase tracking-[.12em] text-slate-400">Inicio laboral</p><p className="mt-2 font-semibold text-slate-800">{form.fechaIngreso || 'Fecha por definir'}</p><p className="mt-1 text-xs text-slate-500">{form.sede} · {form.modalidad}</p></section></div><div className="mt-5 flex gap-3 rounded-2xl bg-emerald-50 p-4 text-sm text-emerald-900"><CheckCircle2 size={19} className="mt-0.5 shrink-0 text-emerald-600"/><p>La información se guardará como una ficha de afiliación pendiente de validación por Recursos Humanos.</p></div></>
}

function Summary({ form, step }) {
  const fullName = `${form.nombres} ${form.apellidos}`.trim()
  return <aside className="h-fit rounded-3xl bg-[#1d1b35] p-6 text-white shadow-xl shadow-violet-950/10 lg:sticky lg:top-8"><div className="flex items-center justify-between"><span className="rounded-full bg-white/10 px-3 py-1 text-[11px] font-semibold text-violet-100">Nueva afiliación</span><span className="text-lg">✨</span></div><div className="mt-6 flex h-14 w-14 items-center justify-center rounded-2xl bg-gradient-to-br from-violet-400 to-fuchsia-400 text-xl font-bold shadow-lg shadow-violet-900/30">{fullName ? fullName.split(' ').map(x=>x[0]).slice(0,2).join('') : 'RH'}</div><h2 className="mt-4 text-lg font-semibold">{fullName || 'Ficha de personal'}</h2><p className="mt-1 text-sm text-violet-200">{form.cargo || 'Cargo por definir'}</p><div className="my-6 h-px bg-white/10"/><div className="space-y-4"><div><p className="text-[10px] font-bold uppercase tracking-[.14em] text-violet-300">Progreso</p><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-gradient-to-r from-violet-300 to-fuchsia-300 transition-all" style={{width:`${step*33.333}%`}} /></div><p className="mt-2 text-xs text-violet-200">Paso {step} de 3 · {STEPS[step-1].label}</p></div><SummaryRow label="Pensión" value={form.sistemaPension==='AFP' ? `AFP ${form.afp}` : form.sistemaPension}/><SummaryRow label="Salud" value={form.seguroSalud}/><SummaryRow label="Ingreso" value={form.fechaIngreso || 'Pendiente'}/></div><div className="mt-7 rounded-2xl bg-white/8 p-4"><div className="flex gap-2"><CircleHelp size={16} className="mt-0.5 shrink-0 text-violet-300"/><p className="text-xs leading-5 text-violet-100">Puedes guardar el registro y completar documentos o derechohabientes después.</p></div></div></aside>
}
function SummaryRow({ label, value }) { return <div className="flex items-center justify-between gap-3"><span className="text-xs text-violet-200">{label}</span><span className="text-right text-xs font-semibold text-white">{value}</span></div> }

function App() {
  const [step, setStep] = useState(1)
  const [form, setForm] = useState(initialForm)
  const [saved, setSaved] = useState(false)
  const set = (key,value) => { setForm(current => ({...current,[key]:value})); setSaved(false) }
  const validFirstStep = useMemo(() => Boolean(form.documento && form.nombres && form.apellidos && form.correo), [form])
  const next = () => { if(step===1 && !validFirstStep) return; setStep(current=>Math.min(3,current+1)) }
  const save = () => { setSaved(true); setStep(3) }
  const disabled = step===1 && !validFirstStep
  return <div className="min-h-screen bg-[#f7f7fb] font-sans text-slate-900"><header className="border-b border-slate-200/80 bg-white"><div className="mx-auto flex h-18 max-w-7xl items-center justify-between px-5 py-4 lg:px-8"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-violet-600 text-sm font-bold text-white shadow-lg shadow-violet-200">P</div><div><p className="font-semibold tracking-[-.02em] text-slate-900">PeopleFlow</p><p className="text-[11px] text-slate-400">Recursos Humanos</p></div></div><div className="hidden items-center gap-2 rounded-full bg-slate-50 px-3 py-1.5 text-xs text-slate-500 sm:flex"><ShieldCheck size={14} className="text-emerald-500"/>Datos protegidos</div></div></header><main className="mx-auto max-w-7xl px-5 py-8 lg:px-8 lg:py-12"><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><button className="mb-4 flex items-center gap-1.5 text-sm font-medium text-slate-500 hover:text-slate-800"><ArrowLeft size={16}/>Personal</button><div className="flex items-center gap-2"><span className="text-xl">👋</span><p className="text-sm font-medium text-violet-600">Nueva incorporación</p></div><h1 className="mt-2 text-3xl font-semibold tracking-[-.035em] text-slate-950 sm:text-4xl">Afiliación de personal</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-500">Registra la cobertura previsional y de salud de un nuevo colaborador en pocos pasos.</p></div>{saved && <div role="status" className="flex items-center gap-2 rounded-2xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-medium text-emerald-800"><CheckCircle2 size={18}/>Ficha guardada como pendiente de validación</div>}</div><div className="grid gap-7 lg:grid-cols-[minmax(0,1fr)_330px]"><section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-8"><Stepper step={step} goTo={setStep}/>{step===1 && <PersonalStep form={form} set={set}/>} {step===2 && <AffiliationStep form={form} set={set}/>} {step===3 && <ReviewStep form={form}/>}<div className="mt-9 flex items-center justify-between border-t border-slate-100 pt-5"><button type="button" onClick={()=>setStep(current=>Math.max(1,current-1))} disabled={step===1} className="inline-flex items-center gap-2 rounded-xl px-3 py-2.5 text-sm font-semibold text-slate-500 transition hover:bg-slate-50 disabled:invisible"><ArrowLeft size={17}/>Anterior</button>{step<3 ? <button type="button" onClick={next} disabled={disabled} className="inline-flex items-center gap-2 rounded-xl bg-violet-600 px-5 py-3 text-sm font-semibold text-white shadow-lg shadow-violet-200 transition hover:bg-violet-700 disabled:cursor-not-allowed disabled:bg-slate-200 disabled:shadow-none">Continuar<ArrowRight size={17}/></button> : <button type="button" onClick={save} className="inline-flex items-center gap-2 rounded-xl bg-violet-600 px-5 py-3 text-sm font-semibold text-white shadow-lg shadow-violet-200 transition hover:bg-violet-700"><FileText size={17}/>Guardar afiliación</button>}</div>{disabled && <p className="mt-3 text-right text-xs text-amber-600">Completa identificación, nombre y correo para continuar.</p>}</section><Summary form={form} step={step}/></div><p className="mt-8 text-center text-xs text-slate-400">Vista conceptual de diseño · PeopleFlow HR</p></main></div>
}

createRoot(document.getElementById('root')).render(<App />)
