// StorageWidget — retirado en Fase A (el backend maneja el almacenamiento)
// Se reintegrará en Fase I si se requiere monitoreo de cuota de API o archivos.
export default function StorageWidget() {
  return null
}
